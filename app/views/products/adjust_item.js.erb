<% item = current_cart.find_item_by(@product) %>
<% if !item %>
  $('.product-<%= @product.id %>').remove();
<% else %>
  $('.total-<%= item.product.id %>-count').text("<%= item.item_total %>");
  $('.cart-<%= item.product.id %>-qty').text("<%= item.quantity %>");
<% end %>

$('.amount').text('<%= current_cart.subtotal %>');

<% if nothing_in_cart? %>
  $('#cart-button').empty();
<% end %>